/**
 * @ngdoc controller
 * @name app.pages.layout.layoutCtrl
 * @restrict EA
 *
 * @description
 * Global functions that common to all (splash, catalog, checkout, product details )
 * differnt parts of the site
 * Primarily the controller offers toggling, getting or finding user / org id, navigating and colors
 *
 * @requires $rootScope
 * @requires $scope
 * @requires $state
 * @requires $stateParams
 * @requires ecomComponentLoader
 * @requires checkoutDataFactory
 */
/* eslint-disable no-param-reassign */
function layoutController() {}

layoutController.$inject = [];

export default layoutController;
